document.addEventListener("DOMContentLoaded", () => {
    // Example of smart visual effect with JavaScript
    const heroSection = document.querySelector('.hero-section');
    window.addEventListener('scroll', () => {
        let scrollPosition
